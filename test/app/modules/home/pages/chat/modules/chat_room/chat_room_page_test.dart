import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('ChatRoomPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatRoomPage(title: 'ChatRoom')));
    //  final titleFinder = find.text('ChatRoom');
    //  expect(titleFinder, findsOneWidget);
  });
}
